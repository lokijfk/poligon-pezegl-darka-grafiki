

using System.Windows;
using System.Windows.Controls;
using System.Windows.Media;

namespace poligon_pezeglądarka_grafiki.View.ext;

public static class TextBlockExtensions
{
    /*
    public static ListBoxItem GetParentAsListBoxItem(this TextBlock block)
    {
        DependencyObject item = block;
        while (item != null && !(item is ListBoxItem))
        {
            item = VisualTreeHelper.GetParent(item);
        }
        return item as ListBoxItem;
    }

    public static ListBox GetParentAsListBox(this TextBlock block)
    {
        DependencyObject item = block;
        while (item != null && !(item is ListBox))
        {
            item = VisualTreeHelper.GetParent(item);
        }
        return item as ListBox;
    }

    /*
    public static Grid GetParentAsGrid(this TextBlock block)
    {
        DependencyObject item = block;
        while (item != null && !(item is Grid))
        {
            item = VisualTreeHelper.GetParent(item);
        }
        return item as Grid;
    }*/
}
